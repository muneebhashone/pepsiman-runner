{
  "name": "pepsiman-runner",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/syntax-check.mjs && node scripts/test-game-systems.mjs && node scripts/sim-spawn-contact.mjs && node scripts/test-cue-queue.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
